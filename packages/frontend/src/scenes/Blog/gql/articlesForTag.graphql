query getArticlesForTag($name: String!) {
  getArticlesForTag(name: $name, offset: 0, limit: 20) {
    id
    title
    content
    slug
    image
    excerpt
    createdAt
    userId
    category {
      id
      name
      slug
      icon
    }
    tags {
      id
      name
    }
  }
}
