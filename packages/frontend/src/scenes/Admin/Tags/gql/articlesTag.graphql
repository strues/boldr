query tags($name: String, $offset: Int, $limit: Int) {
  getArticlesForTag(name: $name, offset: $offset, limit: $limit) {
    id
    title
    slug
    image
    featured
    published
    createdAt
    excerpt
    userId
    tags {
      id
      name
    }
  }
}
