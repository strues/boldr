query getMenuById($id: ID!) {
  getMenuById(id: $id) {
    id
    name
    details {
      id
      safeName
      title
      cssClassname
      icon
      hasDropdown
      isDropdown
      order
      mobileHref
      href
      menuId
      parentId
    }
  }
}
