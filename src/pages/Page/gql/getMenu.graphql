query getMenuById($id: ID!) {
  getMenuById(id: $id) {
    id
    name
    details {
      id
      safeName
      title
      cssClassname
      icon
      hasDropdown
      order
      mobileHref
      href
      children
    }
  }
}
