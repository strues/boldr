mutation editArticle($id: UUID!, $input: EditArticleInput!) {
  editArticle(id: $id, input: $input) {
    title
    slug
    content
    rawContent
    featured
    published
    excerpt
    image
  }
}
