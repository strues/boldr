query GetArticlesQuery($offset: Int!, $limit: Int!) {
  articles(offset: $offset, limit: $limit) {
    id
    title
    slug
    image
    featured
    published
    createdAt
    excerpt
    tags {
      id
      name
    }
  }
}
