query getArticleBySlug($slug: String!) {
    getArticleBySlug(slug: $slug) {
      id
      title
      content
      slug
      image
      createdAt
      userId
      tags {
        id
        name
      }
    }
}
